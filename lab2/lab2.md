### In this lab I learned how to Demonstrate, numerically, that stacked linear layers collapse into a single layer.
and Implement **ReLU**, **sigmoid** and **softmax**, and choose the right one for a given layer.
Build a reusable `Layer_Dense` class and stack it into a feedforward network.
Measure how wrong a network is using **categorical cross-entropy** loss.
Estimate a derivative numerically and use it to run **gradient descent**.
Hand-compute a **backward pass** through a single neuron using the chain rule.
