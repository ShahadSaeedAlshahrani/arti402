In this lab I learned how to use the vocabulary of deep learning correctly. Also, how to compute the output of a single neuron in plain Python.
And explain what a weight does and what a bias does, and show it on a graph. Compute the output of a whole layer — first with loops, then with NumPy.
Predict the shape of every array in a forward pass before you run it.
Count the trainable parameters of a fully connected network.
